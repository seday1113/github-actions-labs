name: Steps Lab

on:
  workflow_dispatch:

jobs:

  step-demo:

    runs-on: ubuntu-latest

    steps:

      - name: Step 1
        run: echo "Starting Build"

      - name: Step 2
        run: sleep 10

      - name: Step 3
        run: echo "Build Complete"
