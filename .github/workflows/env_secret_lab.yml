name: Environment Secret Lab

on:
  workflow_dispatch:

jobs:

  env-secret:

    runs-on: ubuntu-latest

    environment: dev

    steps:
      - name: Show Secret
        run: echo "${{ secrets.DB_PASSWORD }}"
